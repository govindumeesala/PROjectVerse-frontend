
import {CreateProjectForm} from "../forms/CreateProjectForm";

export default function CreateProjectPage() {
  return (
    <div style={{ minHeight: "100vh", background: "#f5f6fa", paddingTop: 40 }}>
      <CreateProjectForm />
    </div>
  );
}